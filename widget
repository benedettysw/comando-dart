cada una tienes su funcicion representar deseño y estrutura de inte


child (hijo): text'gg')art


textdirecion:________ltr


build---crear app 


buildcontext (ayudante) busqueda y ubicacion



(herencia) de widget)

https://www.youtube.com/watch?v=TwUE6Zmsp6M

stateless widget (menos)  no se lleva nada 

satatefull wiget (completo) si hereda todo

render objeto (cumpla las carateristica )

children (es una forma común de agregar múltiples widgets como hijos a un widget contenedor)


Expanded (ES PARA EXPADIR tamaño y posición)

extend (establecer una relación de herencia entre dos clases)

clases ( una clase es como un plano o una receta para crear objetos. Cuando se crea una instancia de una clase)

@override (se utiliza para indicar que un método de una clase hija está sobrescribiendo (anulando) un método con el mismo nombre en la clase padre)
