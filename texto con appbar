import 'package:flutter/material.dart';

void main() {
  runApp(Bene());
}

class Bene extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const  Text('Hola'),
           centerTitle: true, 
        ),
      ),
    );
  }
}
